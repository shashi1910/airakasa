{
  "name": "foodmate-platform",
  "version": "1.0.0",
  "description": "Minimal food ordering platform",
  "private": true,
  "scripts": {
    "dev:all": "concurrently \"npm run dev --prefix app/backend\" \"npm run dev --prefix app/frontend\"",
    "install:all": "npm install && npm install --prefix app/backend && npm install --prefix app/frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
